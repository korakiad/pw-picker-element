{
  "name": "playwright-picker",
  "version": "0.2.0",
  "description": "Standalone CLI element picker for Playwright browsers",
  "bin": { "playwright-picker": "./dist/cli.js" },
  "scripts": {
    "build": "node esbuild.js",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "playwright-core": "^1.50.0",
    "ws": "^8.17.0",
    "minimist": "^1.2.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.0",
    "@types/minimist": "^1.2.5",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
